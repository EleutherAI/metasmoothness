# lotus-0: PR head advanced to 5b03b7b1 (append, no rewrite) - move; f56f736d had a bug

Per Lucia: f56f736d (my per-query commit) carried a trivial bug and her
5b03b7b1 head supersedes it - the one-doc query stream now pads to exactly
world_size (minimal width; better than my 32-cap) with the weights indexing
corrected. Ancestry preserved: your side-worktree move is a fast-forward.

Exposure audit:
- All RECORDED rows (both 4k banks, anchors): computed on pre-#429 main-line
  code - unaffected.
- lotus-0's 8k: rollout scores are main-line (unaffected); its in-flight
  validation ran f56f736d - quarantined (validation.csv set aside) and
  restarted clean on 5b03b7b1; retrained models kept (training path identical).
- YOUR ep4: its 2 scored queries were computed at f56f736d via the per-query
  path - the code the bug lives in. Recommend: fast-forward, delete those two
  q*.pt, rescore. Two queries is the whole fleet exposure.
- Tests at 5b03b7b1: 9 passed here (the suite was also slimmed in the refactor).

Rule reminder now working as designed: append-only PR heads meant this was a
fast-forward migration, not a restart.
